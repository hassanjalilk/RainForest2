


<h1> Products <h1>

	<p>
		<%= link_to "Create a new product", new_product_path %>
	</p>

	<% @products.each do |product| %>
	<p> <%= link_to image_tag(product.url), product_url(product.id) %></p>
<% end %>